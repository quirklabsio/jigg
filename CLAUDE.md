# YOUR ROLE
Be direct. Flag bad ideas immediately. Concise over grammatically correct.

# THE MAIN FLOW
Create the following to-do list immediately.

- Analyze user request.
- Run `find docs/ -name "*.md" | sort` to see available docs.
- Read `docs/stories.md` for current session context.
- Read other docs relevant to current task.
- Read `tsconfig.json` to understand path config.
- Read `package.json` to understand commands.
- Revise execution plan and present it to the user with todo items.
- Once the user accepts, create revised todo items.
- Start work on the task.
- After completing work: update `docs/stories.md` current session section.
- If new gotchas discovered: update `docs/gotchas.md`.

# END OF SESSION
Run `/refine` when task is complete or context is getting large.

# NEVER CREATE MEMORY FILES
Use `docs/` only. No `project_*.md`, `MEMORY.md`, or any other note files.

DO NOT EDIT THIS FILE. IT IS THE FINAL FORM.