# No story queued

The accessibility architecture work is in tech-architect's hands, not BA's. A `tech-architect` session designs the target pattern and produces a sequenced backlog of implementable story briefs; BA then writes individual next-story prompts from those briefs.

Run the tech-architect skill on the accessibility architecture (input: `docs/accessibility-architecture.md` audit + the user's HC bench-blackening insight) before queueing the next dev story.
